package src.leetCode.Tree;
/*
public class maxDepthinX {
    public int maxDepth(Node root) {
        int maxChildDepth = 0;      // 存放当前节点的children最大深度
        if(root == null){
            return 0;
        }
       else{
           for(TreeNode child : node.children){     //遍历children，查找每个孩子的最大深度
               int ChildDepth = maxDepth(child);
               maxChildDepth=Math.max(maxChildDepth, ChildDepth);
           }
           return maxChildDepth+1;  // 返回孩子最大深度+1
        }
    }
}
*/